import { describe, test } from 'vitest';

describe('user connector', () => {
  describe('getUsers', () => {
    test('should return users and nextPage when the token is valid and their is another page', () => {
      // TODO
    });

    test('should return users and no nextPage when the token is valid and their is no other page', () => {
      // TODO
    });

    test('should throws when the token is invalid', () => {
      // TODO
    });
  });
});
